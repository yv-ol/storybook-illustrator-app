{
  "name": "personalized-storybook-illustrator",
  "version": "1.0.0",
  "description": "A web application that allows users to write a story page by page and generate a unique, consistent illustration for each page using AI.",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/main.js --format=esm --jsx=automatic"
  },
  "dependencies": {
    "@google/genai": "^1.11.0",
    "html2canvas": "^1.4.1",
    "jspdf": "^3.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.4.5"
  }
}